language: generic
sudo: required
services:
 - docker

vefore_install:
 - docker build -t fnavarro94/docker-react -f  Dockerfile.dev .

script:
 - docker run -e CI=true fnavarro94/docker-react npm run test
